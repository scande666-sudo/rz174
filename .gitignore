# Не тащим в репозиторий

# подборка кадров из Pexels — больше не нужна
_podbor/
# пустая папка, модели лежат в Downloads
модель/
# методичка + пароль к полке референсов — не для репозитория
информация/

# служебный мусор Windows / macOS
Thumbs.db
desktop.ini
.DS_Store
*.tmp
*.bak

# память проекта — храним только заметки, остальное служебное
.workbuddy-ai/*
!.workbuddy-ai/memory/
